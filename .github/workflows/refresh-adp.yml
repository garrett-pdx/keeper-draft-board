name: Refresh ADP and outlook data

on:
  schedule:
    # Monday and Friday, 13:00 UTC (~6am Pacific / 9am Eastern).
    - cron: '0 13 * * 1,5'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run fetch-adp
      - run: npm run fetch-outlooks
      - name: Commit snapshots if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/adp-snapshot.json public/outlook-snapshot.json
          git diff --cached --quiet && echo "No changes." || git commit -m "Refresh ADP and outlook snapshots"
          git push
